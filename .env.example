# Environment Variables for Base64 Encoder Decoder

# ==================== SITE CONFIGURATION ====================
# Site URL - Used for SEO metadata, sitemaps, and canonical URLs
# Change this to your production domain when deploying
NEXT_PUBLIC_SITE_URL=https://base64tool.com

# Site Name - Appears in header, PWA manifest, and SEO metadata
NEXT_PUBLIC_SITE_NAME=Base64 Tool

# Full Site Title - Used in metadata and Open Graph tags
NEXT_PUBLIC_SITE_TITLE=Base64 Encoder & Decoder

# Site Description - Used for SEO meta description
NEXT_PUBLIC_SITE_DESCRIPTION=Free online Base64 encoder and decoder with live sync. Instantly convert text to Base64 and vice versa. No data sent to servers - 100% client-side processing.

# ==================== FEATURE LIMITS ====================
# Maximum file upload size in bytes (default: 1MB = 1048576 bytes)
NEXT_PUBLIC_MAX_FILE_SIZE_BYTES=1048576

# Maximum text input size in bytes (default: 100MB = 104857600 bytes)
NEXT_PUBLIC_MAX_INPUT_SIZE_BYTES=104857600

# Maximum number of history entries to store (default: 100)
NEXT_PUBLIC_MAX_HISTORY_ENTRIES=100

# ==================== UI TIMING (milliseconds) ====================
# Delay before showing PWA install prompt (default: 5000ms = 5 seconds)
NEXT_PUBLIC_INSTALL_PROMPT_DELAY_MS=5000

# Duration to show "back online" indicator (default: 3000ms = 3 seconds)
NEXT_PUBLIC_ONLINE_INDICATOR_DELAY_MS=3000

# Duration to show copy success message (default: 2000ms = 2 seconds)
NEXT_PUBLIC_COPY_SUCCESS_DURATION_MS=2000

# ==================== PWA THEME ====================
# Theme color for PWA and browser UI (hex color)
NEXT_PUBLIC_THEME_COLOR=#7c3aed

# Background color for PWA (hex color)
NEXT_PUBLIC_BACKGROUND_COLOR=#f5f5f7

# ==================== DEVELOPMENT ====================
# Uncomment below for local development
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
